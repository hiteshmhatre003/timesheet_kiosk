[project]
name = "purchase_mobile"
authors = [
    { name = "2nd Innings Handicrafts Private Limited" }
]
description = "Mobile app for recording purchase entries and browsing stock"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
